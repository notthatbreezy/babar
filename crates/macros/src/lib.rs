//! Procedural macros for the `babar` PostgreSQL driver.
//!
//! Empty in M0 — the `sql!` macro arrives in M3 and `#[derive(Codec)]`
//! arrives in M5. The crate exists now so the workspace layout is stable
//! and downstream re-exports can be wired in early.
